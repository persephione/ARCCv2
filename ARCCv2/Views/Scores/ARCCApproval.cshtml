<div class="row" ng-form="form.approvalForm">
    <div class="col-lg-1"></div>
    <div class="col-lg-10 center-block">
        <div class="panel">
            <div class="col-lg-12 panel-heading sliderPanelHeading" style="margin:15px 0 15px 0;">Approval Form</div>
        </div>
    </div>
    <div class="col-lg-1"></div>

    <div class="row">
        <div class="col-lg-12">
            <table class="table table-striped approvalTable">
                <tr>
                    <th width="200px">Member</th>
                    <th>Educ Experience</th>
                    <th>Innovation</th>
                    <th>Dissemination</th>
                    <th>Evaluation</th>
                    <th>Support</th>
                    <th>Total</th>
                </tr>
                <tr ng-repeat="score in model.arccProposalScoreList">
                    <td>{{::score.UserName}}</td>
                    <td>{{::score.ARCCScoreEducExp}}</td>
                    <td>{{::score.ARCCScoreInnovation}}</td>
                    <td>{{::score.ARCCScoreDissemination}}</td>
                    <td>{{::score.ARCCScoreEvaluation}}</td>
                    <td>{{::score.ARCCScoreSupport}}</td>
                    <td><strong>{{::score.ARCCScoreTotal}}</strong></td>
                </tr>
            </table>
        </div>
    </div>

</div>


<div class="row">
    <div class="col-lg-12 center-block" style="margin-top:50px">
        <button class="weberButton weberButtonGreen" ng-click="approval(true)">Approve</button>
        <button class="weberButton weberButtonRed" ng-click="approval(false)">Deny</button>
        <button class="weberButton" ng-click="togglePanels('none')">Close</button>
    </div>
</div>


